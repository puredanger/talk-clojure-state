(ns demo-coll 
  (:use [clojure.core]))

;;;; Vectors

;; Create new vector
;; Add item with conj
;; Check original vector

;;;; Maps 

;; Create favorite fruit - name, kinds
(def apple {:name "apple", :kinds ["Fuji" "Golden Delicioius"]})

;; Add shape - assoc 
;; Check original fruit - apple
;; Keys, vals
;; Retrieving values - get, key on :name
;; Check key with contains?











